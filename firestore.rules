rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Catch-all rules allowing client and server reads/writes
    // since the app uses custom JWT server-side authentication instead of Firebase Auth.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
